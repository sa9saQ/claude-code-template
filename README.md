# Project Template

AI code review workflows included.

## Setup after creating repo from template:

```bash
gh secret set ANTHROPIC_API_KEY
gh secret set OPENAI_API_KEY
```

PRs will be automatically reviewed by Claude and GPT.
